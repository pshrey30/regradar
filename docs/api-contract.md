# API Contract

This document will hold the finalized REST API contract (endpoints, request/response schemas,
error envelope, and webhook verification instructions) as the API-* tickets land.

Placeholder — populated starting with API-01 (FastAPI skeleton + health check).
