"""Local dev entrypoints: run a single filing through the pipeline, run eval suites, etc."""


def main() -> None:
    """Entrypoint placeholder — populated as CLI subcommands land with their owning tickets."""
    print("regradar CLI — no subcommands implemented yet.")


if __name__ == "__main__":
    main()
