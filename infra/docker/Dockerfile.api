FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml alembic.ini ./
COPY src ./src
COPY migrations ./migrations

RUN pip install --no-cache-dir -e ".[api]"

EXPOSE 8000

CMD ["uvicorn", "regradar.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
